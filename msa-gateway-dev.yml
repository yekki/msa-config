
server:
  port: 8888

zuul:
  routes:
    user:
      path: /api/**
      serviceId: msa-user-provider
      sensitiveHeaders:
  ignored-patterns:
    - /api/hello
management:
  security:
    enabled: false
