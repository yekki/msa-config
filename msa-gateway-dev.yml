
server:
  port: 8888

zuul:
  routes:
    user:
      path: /api/**
      serviceId: msa-user-provider
      sensitiveHeaders:
  ignored-patterns:
    - /**/msa-user-provider/hello
management:
  security:
    enabled: false
